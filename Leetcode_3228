Question 
3228. Maximum Number of Operations to Move Ones to the End
link - https://leetcode.com/problems/maximum-number-of-operations-to-move-ones-to-the-end/description/
Intuition - Count zero-groups, and for each 1, add how many zero-groups are on its right.

class Solution {
    public int maxOperations(String s) {
        int n = s.length();
        int k = n-1;
        for(int i = n-1; i>=0; i--){
            if(s.charAt(i) == '0') break;

            k--;
        }

        int operations = 0;
        int zeroCount = 0;
        
        while(k >= 0){
            boolean zero = false;
            while(k>=0 && s.charAt(k) == '0'){
                zero = true;
                k--;
            }
            if(zero) zeroCount++;

            while(k>=0 && s.charAt(k) == '1'){
                operations += zeroCount;
                k--;
            }
        }

        return operations;
    }
}
